name: Service Contract Boundary

on:
  pull_request:
  push:

jobs:
  service-contract-boundary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject microservice implementation dependencies
        run: ./scripts/check-service-implementation-dependencies.sh
