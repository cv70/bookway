# 后台消费者

长期运行的成长事件消费者放在这里，并与 HTTP 服务独立部署。

- `entry-publication-dispatcher/`：消费用户主动请求公开的成长记录，幂等创建并审核社区行记；以数据库租约、指数退避和终态失败状态保证客户端重试或 worker 崩溃不会制造重复内容。
- `push-delivery-dispatcher/`：消费已持久化的行动提醒投递，以租约、指数退避和 Provider 幂等键发送推送；发送前锁定并复核行动版本、路线状态与设备有效性，失效设备会被撤销而非无限重试。
