syntax = "proto3";
package bookway.user.event;

message IngestRequest { string user_id = 1; string request_json = 2; }
message JsonResponse { string response_json = 1; }

service UserEvent {
  rpc Ingest(IngestRequest) returns (JsonResponse);
}
